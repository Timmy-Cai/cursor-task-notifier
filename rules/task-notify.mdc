---
description: 大型任务开始前询问是否开启完成通知
alwaysApply: true
---

# 任务完成通知提醒

## 判断标准

满足以下任意一条，视为「大型任务」，需要提前询问：
- 需要修改 3 个及以上文件
- 涉及新功能实现、架构调整、批量重构
- 用户使用 /tb-apply、/tb-tasks、/tb-plan 等工作流指令
- 预计需要多轮工具调用（超过 5 次）

## 行为规范

检测到大型任务时，**在开始执行前**读取通知配置状态：

```bash
cat ~/.cursor/hooks/task-done.conf
```

根据当前 `NOTIFY_ENABLED` 的值决定如何提示：

- **当前为 `false`（通知关闭）**：询问用户
  > 这个任务预计耗时较长，要我完成后通知你吗？回复「开启」我帮你打开。

- **当前为 `true`（通知已开启）**：无需询问，直接开始任务

用户回复「开启」时执行：
```bash
sed -i '' 's/NOTIFY_ENABLED=false/NOTIFY_ENABLED=true/' ~/.cursor/hooks/task-done.conf
```

## 限制

- 每次对话只询问一次，不重复询问
- 小任务（单文件修改、代码查询、问答）直接执行，不询问
- 用户明确说「不用」「不需要」则跳过，直接开始
